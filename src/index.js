const coinCounter = require('./coinCounter/coinCounter')
const intersection = require('./intersection/intersection')
const removeDuplicates = require('./removeDuplicates/removeDuplicates')
const reverseInteger = require('./reverseInteger/reverseInteger')
const twoSum = require('./twoSum/twoSum')

module.exports = {
    coinCounter,
    intersection,
    removeDuplicates,
    reverseInteger,
    twoSum
}